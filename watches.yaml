---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: apps.erdemo.io
  kind: ErDemo
  playbook: playbooks/install.yml
  finalizer:
    name: finalizer.erdemo.io
    vars:
      ACTION: uninstall
# +kubebuilder:scaffold:watch
